import React from 'react';
import { expect } from 'chai';
import MyComponent from './my-component';

describe('Spec MyComponent', function () {
    it('it exists', () => {
        expect(MyComponent).to.be.ok;
    });
});
